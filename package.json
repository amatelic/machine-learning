{
  "name": "ma",
  "version": "0.0.1",
  "description": "Site for ma and ai algorithems",
  "main": "main.js",
  "scripts": {
    "server": "json-server ./data.json & live-server & gulp sass:watch & nodemon --harmony_object_values_entries ./server/index.js",
    "sass": "sass --watch ./sass:public",
    "sass-alg": "gulp sass:watch",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Anže Matelič <amatelic93@gmail.com>",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.15.0",
    "express": "^4.13.4"
  },
  "devDependencies": {
    "gulp": "^3.9.1",
    "gulp-sass": "^2.3.1"
  }
}
